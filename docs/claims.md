<!-- Generated by tools/refcheck.py --ledger. Do not edit. -->

# The claim ledger

Every factual claim every lesson makes, with the evidence for it. Written against `llvmorg-23.1.0`.

`observed` means somebody ran it, on two platforms. `cited` means it is in the LLVM source or its documentation at the pinned tag, at the line given. `inferred` means neither, and a lesson is allowed at most three.

139 claims: 81 observed, 56 cited, 2 inferred.

## t01_one_line_of_c

| Claim | Confidence | Evidence |
|---|---|---|
| clang -O0 turns a one line add into eight instructions, two allocas, two stores, two loads, an add and a ret, and clang -O2 turns the same C into two. | observed | cell source<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, eight and two on both |
| clang marks every function optnone at -O0 and does not at -O2. | observed | cell find_the_note<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0 |
| Running the whole default<O2> pipeline over clang's own -O0 output leaves the function byte for byte unchanged. | observed | cell opt_does_nothing<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, eight instructions in and eight out on both |
| optnone is not checked by each pass. OptNoneInstrumentation::shouldRun is consulted by the pass manager before any pass runs on a function, so a pass that has never heard of optnone is skipped anyway. | cited | cell watch_the_skips<br>llvm/lib/Passes/StandardInstrumentations.cpp:1039-1053@llvmorg-23.1.0 |
| default<O2> over an optnone function skips 76 passes, and SROAPass, the pass that would have removed the stack slots, is one of them. | observed | cell watch_the_skips<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, 76 on both, SROAPass third in the list on both |
| The IR verifier rejects a module carrying optnone without noinline, with the message "Attribute 'optnone' requires 'noinline'!". | cited | cell watch_the_skips, and opt -passes=verify over a hand written define with optnone alone<br>llvm/lib/IR/Verifier.cpp:2363-2368@llvmorg-23.1.0, message reproduced on both platforms |
| Deleting optnone and noinline from the attribute group, changing nothing else, makes the same default<O2> pipeline collapse the eight instructions to the same two that clang -O2 produced. | observed | cell strip_the_note<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0 |
| irx.compile_c passes -Xclang -disable-O0-optnone, so IR from the toolkit has no optnone on it and passes run on it, which is a deliberate departure from what clang does by default. | cited | cell the_toolkit_flag<br>toolkit/irx/ir.py, Module.from_c |

## t02_first_look_at_the_ir

| Claim | Confidence | Evidence |
|---|---|---|
| A module's top level is target description lines, globals, functions with bodies, functions without, then attribute groups and metadata. @ starts a global, % something local to a function, ! metadata. | observed | cell whole_module<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, same set of kinds on both |
| A basic block is a label, then instructions, then exactly one terminator, which is the last instruction. A block with no terminator or with a terminator in the middle is not accepted. | cited | cell inside_a_function<br>llvm/docs/LangRef.md:1038-1043 and 9968-9971@llvmorg-23.1.0 |
| The pointer type is ptr and carries no pointee type. Four C functions taking int *, char *, a pointer to a 128 byte struct and a function pointer all compile to the identical LLVM signature ptr (ptr). | observed | cell four_pointers<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, four identical signatures on both, and llvm/docs/LangRef.md:4713-4718@llvmorg-23.1.0, whose whole Pointer Type section describes address spaces and representation width and never mentions a pointee |
| The old typed pointer syntax i32* still parses. The parser translates it to ptr and the i32 is gone from what LLVM stores, so material written before the change does not visibly break. | observed | cell old_syntax<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, both print back ptr |
| Typed pointers forced the front end to emit no-op bitcasts purely to satisfy the type system, cost memory and compile time, and made passes miss optimisations when they failed to look through one. Removing the pointee type removed that whole category. | cited | cell old_syntax, and the prose that follows it<br>llvm/docs/OpaquePointers.rst:78-90@llvmorg-23.1.0 |
| Changing a load through an int * argument from i32 to i64, and changing nothing else but the return type, produces IR the verifier accepts, because no record of the C pointee type survives in the file. | observed | cell nothing_checks_it<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, verifier exits 0 on both |
| The type on a getelementptr supplies a stride rather than describing memory. The same C array index is emitted over i32 at -O0 and over [4 x i8] at -O1, both striding four bytes per index to the same address. | observed | cell gep_is_arithmetic<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, i32 at -O0 and [4 x i8] at -O1 on both |
| Unnamed register numbers are not stored. SlotTracker::processFunction walks one counter over unnamed arguments, then for each block the block itself if unnamed, then each unnamed instruction whose type is not void, and the printer uses the result. | cited | cells who_names_the_registers and void_takes_nothing<br>llvm/lib/IR/AsmWriter.cpp:1143-1166@llvmorg-23.1.0, CreateFunctionSlot at 1408-1417 |
| Adding the label entry: to the entry block changes every number in the function, because an unnamed block consumes a slot and a named one does not. The same one line function prints %1 without the label and %0 with it. | observed | cell who_names_the_registers<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, %1 and %0 on both, and stated in llvm/docs/LangRef.md:1043-1048@llvmorg-23.1.0 with the same worked example |
| store and ret produce no value and take no slot. A seven instruction function written with the numbers 0, 2 and 4 prints back as 0, 1 and 2. | observed | cell void_takes_nothing<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, identical output on both |
| The parser accepts any unnamed value number at least as large as the one it expects and renumbers on output, so %5 and %99 and %4 are all accepted, and a number that is too small is an error reading "instruction expected to be numbered '%N' or greater". | cited | cells who_names_the_registers, void_takes_nothing and the_error<br>llvm/lib/AsmParser/LLParser.cpp:3595-3601, LLParser::checkValueID@llvmorg-23.1.0, message reproduced on both platforms |
| In a function with two unnamed arguments and an unlabelled entry block, the arguments take %0 and %1, the block takes %2, and the first instruction must be %3, which is why writing %2 there is rejected. | observed | cell the_error<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, same message and same number on both |

## t03_reading_ll

| Claim | Confidence | Evidence |
|---|---|---|
| Every block label in clang's output carries a generated "; preds =" comment listing the blocks that can branch to it, and a loop header is a block with a predecessor that appears below it. | observed | cell the_loop_at_o0<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, for.cond lists for.inc and entry on both |
| Control flow at this level is only blocks and two shapes of br, an unconditional br label and a conditional br i1 that takes a one bit condition and two labels, true first. | cited | cell the_loop_at_o0<br>llvm/docs/LangRef.md, the br instruction@llvmorg-23.1.0 |
| SSA constrains registers and not memory. A stack slot may be stored to any number of times, which is how clang emits a loop that assigns total on every iteration while every register in the function is assigned once. | observed | cell the_loop_at_o0<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, three allocas and eight load or store instructions and no phi at -O0 on both |
| Running mem2reg alone on the -O0 loop removes all three allocas and the eight load and store instructions and produces two phi instructions at the top of the loop header, leaving the five blocks and the branches between them unchanged. | observed | cell mem2reg<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, identical output on both apart from dso_local |
| A phi is a list of value and label pairs, one for each predecessor of its block, and the value it takes is the one paired with the block control arrived from. | cited | cell mem2reg<br>llvm/docs/LangRef.md:13370-13372@llvmorg-23.1.0, "the phi instruction logically takes on the value specified by the pair corresponding to the predecessor basic block that executed" immediately before the current one |
| A swap inside a loop compiles, after mem2reg, to two phi instructions in the same block each of which names the other, and the first of them names a value defined on the line below it. | observed | cell the_swap<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, the same two lines in the same order on both |
| Executing that IR with lli gives 20, 10 and 20 for one, two and three iterations, so the pair really alternates. Reading the two phis as assignments in order predicts 10 for every count above one. | observed | cell run_it<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, exit codes 20, 10, 20 on both |
| The phis in a block all take effect together on the edge into the block, and each reads its incoming value as it was in the predecessor. LangRef puts the use of each incoming value on the edge from the predecessor rather than at the point in the block where the phi appears. | cited | cells the_swap and run_it<br>llvm/docs/LangRef.md:13358-13361@llvmorg-23.1.0 |
| Because a phi's use of an incoming value happens on the edge rather than inside the block, a phi may name a value defined below it, while an ordinary instruction that does the same is rejected with "Instruction does not dominate all uses!". | cited | cells the_swap and the_two_rules<br>llvm/lib/IR/Verifier.cpp:5211@llvmorg-23.1.0, message reproduced on both platforms |
| A phi may not have an ordinary instruction above it in its block. The verifier rejects that with "PHI nodes not grouped at top of basic block!", and the rule is what makes the dominance exemption well defined. | cited | cell the_two_rules<br>llvm/lib/IR/Verifier.cpp:3804@llvmorg-23.1.0 and llvm/docs/LangRef.md:13354-13356@llvmorg-23.1.0, "There must be no non-phi instructions between the start of a basic block and the PHI instructions", message reproduced on both platforms |

## t04_the_pass_tape

| Claim | Confidence | Evidence |
|---|---|---|
| One opt run with --print-changed and --print-module-scope gives the ordered pass list, which passes changed the IR, and the whole module after each pass that did. | observed | cell first_tape<br>llvm/lib/Passes/StandardInstrumentations.cpp@llvmorg-23.1.0 |
| opt writes five different "*** IR ... ***" banner shapes, and only one of them has a module underneath it. | cited | cell banners<br>llvm/lib/Passes/StandardInstrumentations.cpp:475-506@llvmorg-23.1.0 |
| opt reports pass managers and adaptors with the word "ignored", which is what lets irx leave them out of the pass count. | cited | cell banners<br>llvm/lib/Passes/StandardInstrumentations.cpp:503-506@llvmorg-23.1.0 |
| In default<O2> the pass that deletes the duplicate load in twice is EarlyCSE, not InstCombine and not GVN. | observed | cell twice_tape, and cell three_alone for the three run separately<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.1, EarlyCSE at pass 10 on both |
| GVN runs on twice in default<O2> and changes nothing, because EarlyCSE has already taken the redundancy. | observed | cell twice_tape<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.1, GVN at pass 45 on both, marked unchanged |
| EarlyCSE, InstCombine and GVN each remove the duplicate load when run alone after SROA, so which one gets the job is decided by pipeline order and nothing else. | observed | cell three_alone<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.1 |
| EarlyCSE is placed near the start of the simplification pipeline under the comment "Catch trivial redundancies", and GVN is added hundreds of lines later under "Eliminate redundancies". | cited | quoted in the lesson<br>llvm/lib/Passes/PassBuilderPipelines.cpp:504-510 and 779-784@llvmorg-23.1.0 |
| InstCombine changes twice after the load is already gone, turning "add nsw i32 %0, %0" into "shl nsw i32 %0, 1". | observed | cell three_alone<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.1 |
| In default<O2> the pass that removes the loop in gauss is LoopDeletion, not GVN, and running LoopDeletion by hand on the frame IndVarSimplify printed takes the module from 48 lines with one loop to 41 lines with none. | observed | cell blame_gvn<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.1, identical line counts on both |
| The tape reports a delta of minus ten lines for GVN, of which seven belong to LoopDeletion and three to GVN itself. | observed | cell blame_gvn<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.1 |
| A loop pass that deletes its loop is reported through runAfterPassInvalidated, which takes no IR, so opt prints a banner with no module and no scope. | cited | cell find_deletion shows the resulting row, with changed true and no dump<br>llvm/lib/Transforms/Scalar/LoopPassManager.cpp:289-295 and llvm/lib/Passes/StandardInstrumentations.cpp:490-493@llvmorg-23.1.0 |
| LoopDeletion emits the optimisation remark "Loop deleted because it is invariant", which -pass-remarks=loop-delete will show you. | cited | cell remark<br>llvm/lib/Transforms/Scalar/LoopDeletion.cpp:504-509@llvmorg-23.1.0 |
| IndVarSimplify makes gauss five lines longer by writing the sum as arithmetic on n, using i33 values so the multiply cannot overflow before the shift. | observed | cell first_tape, the IndVarSimplify frame<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.1, delta plus five on both |
| The growth is what makes the deletion legal, because a loop is only deletable once nothing outside it needs the value it was computing. | inferred | the order of the two passes in cell blame_gvn<br>llvm/lib/Transforms/Scalar/LoopDeletion.cpp:496-513@llvmorg-23.1.0 tests isLoopDead before deleting |
| The position of a pass in the pipeline depends on the target, so the same pipeline over the same function gives 119 passes on macOS arm64 and 118 on Linux x86_64, and every claim above is written to survive that. | observed | docs/diagrams/o2-passes.json records the macOS run, the Linux run was done by hand<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.1 |

## t05_the_pipeline_string

| Claim | Confidence | Evidence |
|---|---|---|
| sroa,indvars,loop-deletion leaves sum_to with the entry block and for.end and no loop, while sroa,loop-deletion,indvars leaves all five blocks and the loop intact. Neither is an error. | observed | cell same_names_new_order<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, 9 instructions in 2 blocks against 12 in 5 on both |
| loop-deletion eliminates loops with a computable trip count that have no side effects and do not contribute to the return value, so the loop in sum_to is not dead until indvars has rewritten what comes after it. Running loop-deletion first therefore wastes it, and a pipeline entry runs once with no second chance. | cited | cell same_names_new_order<br>llvm/lib/Transforms/Scalar/LoopDeletion.cpp:9-12@llvmorg-23.1.0, "eliminating loops with non-infinite computable trip counts that have no side effects or volatile instructions, and do not contribute to the computation of the function's return value" |
| opt reads the first name in a -passes list, decides which level that pass belongs to, and wraps the whole list at that level, so loop-deletion,indvars,sroa is parsed as a loop pipeline and fails with "unknown loop pass 'sroa'". | cited | cells the_order_that_will_not_parse and where_a_pass_lives<br>llvm/lib/Passes/PassBuilder.cpp:2715-2719@llvmorg-23.1.0, "If the first name isn't at the module layer, wrap the pipeline up automatically", and llvm/docs/NewPassManager.md:449-457@llvmorg-23.1.0 |
| The message is produced by the pipeline parser rather than by any pass, and there is one per level, so the wording tells you which level opt had settled on. | cited | cell where_a_pass_lives<br>llvm/lib/Passes/PassBuilder.cpp:2553@llvmorg-23.1.0 for the loop wording and llvm/lib/Passes/PassBuilder.cpp:2486@llvmorg-23.1.0 for the function wording |
| The levels nest as module, then optionally call graph component, then function, then loop. A list may go from a wider level to a narrower one and never back, which is why globaldce,instcombine runs and instcombine,globaldce does not. | cited | cell where_a_pass_lives<br>llvm/docs/NewPassManager.md:445@llvmorg-23.1.0, "The nesting is: module (-> cgscc) -> function -> loop, where the CGSCC nesting is optional" |
| A narrower pass named in a wider list is wrapped in an adaptor, and each name gets its own, so globaldce,instcombine,loop-deletion becomes globaldce, then function(instcombine), then a separate function(loop(loop-deletion)). | cited | cell print_the_pipeline<br>llvm/docs/NewPassManager.md:459-465@llvmorg-23.1.0 and llvm/lib/Passes/PassBuilder.cpp:2173@llvmorg-23.1.0 |
| Two adaptors mean two walks over the functions of the module, and one pass manager holding both passes means one walk that runs both on each function. Upstream recommends the grouped form for cache locality and says it can change the quality of the optimisation. | cited | cell print_the_pipeline<br>llvm/docs/NewPassManager.md:112-140@llvmorg-23.1.0 |
| The pipeline opt builds from -passes=sroa is function(sroa<modify-cfg>),verify, so the printed form shows the adaptor, the pass parameters filled in with their defaults, and a verify run that opt appends. | observed | cell print_the_pipeline<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, byte identical on both |
| default<O2> prints as a single pipeline text of about 4800 characters. The exact length and entry count are target dependent, 4791 characters and 120 entries on macOS arm64 and 4770 and 119 on Linux x86_64, which is why the lesson prints them rather than stating them. | observed | cell the_o2_string<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0 |
| Feeding the printed default<O2> text back to -passes= produces byte identical IR to running default<O2> by name, and opt checks this itself by building a pipeline from the text before printing it. | cited | cell feed_it_back<br>llvm/tools/opt/NewPMDriver.cpp:171-176@llvmorg-23.1.0, the -disable-pipeline-verification flag whose description is that it "Disables verifying that the textual pipeline generated by -print-pipeline-passes can be used to create a pipeline" |
| simplifycfg appears eight times in default<O2> with five distinct sets of options. Seven of the eight are given keep-loops, three are given switch-to-arithmetic, and exactly one, the seventh, is given switch-to-lookup. | observed | cell eight_simplifycfgs<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, the same eight instances in the same positions on both |
| On a six case switch, simplifycfg<no-switch-to-lookup> leaves the switch and its arms, and simplifycfg<switch-to-lookup> produces a private constant array named @switch.table.pick and a function that is a range check and a load. | observed | cell one_option_one_table<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, 23 instructions and no global against 11 and one on both |

## t06_who_says_it_is_stale

| Claim | Confidence | Evidence |
|---|---|---|
| opt -passes=licm,loop-rotate runs LoopSimplifyPass, LCSSAPass, LICMPass, LoopRotatePass and VerifierPass, so three of the five are passes nobody typed. | observed | cell two_passes_four_ran<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, the same five names in the same order on both |
| The two extra loop passes come from the loop adaptor's constructor, which adds LoopSimplifyPass and LCSSAPass to a canonicalisation pipeline it runs before the loop passes it was given. | cited | cell two_passes_four_ran<br>llvm/include/llvm/Transforms/Scalar/LoopPassManager.h:404-410@llvmorg-23.1.0, "LoopCanonicalizationFPM.addPass(LoopSimplifyPass()); LoopCanonicalizationFPM.addPass(LCSSAPass());" in the constructor |
| -print-pipeline-passes and -debug-pass-manager describe the same request differently and both are right. The printed pipeline for licm,loop-rotate names neither canonicalisation pass, because they are inside the adaptor rather than in the pipeline, and it names the loop-mssa adaptor rather than loop. | observed | cells two_passes_four_ran and the_printed_pipeline_does_not_know<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, byte identical printed pipeline on both |
| An analysis is computed because a pass asked the analysis manager for it. The manager returns a cached result if it has one that is still valid, and otherwise runs the analysis, caches it and returns that. | cited | cells an_analysis_asks_for_an_analysis and asked_twice_computed_once<br>llvm/docs/NewPassManager.md:189-195@llvmorg-23.1.0, "Querying for an analysis will cause the manager to check if it has already computed the result for the requested IR" |
| require<loops> causes four analyses to run, of which LoopAnalysis and DominatorTreeAnalysis are the substance, and LoopAnalysis is printed first because the print happens before an analysis starts rather than after it finishes. So the log is nesting rather than sequence. | cited | cell an_analysis_asks_for_an_analysis<br>llvm/lib/Passes/StandardInstrumentations.cpp:1157-1160@llvmorg-23.1.0, the "Running analysis: " print is registered as a before-analysis callback, and the same four names on both platforms |
| In require<domtree>,require<loops>,require<domtree> the dominator tree is computed once, for two passes that demand it directly and a LoopAnalysis that needs one as well. | observed | cell asked_twice_computed_once<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, one "Running analysis: DominatorTreeAnalysis" line on both |
| Over sum_to and twice, simplifycfg changes sum_to and the dominator tree is dropped, changes nothing in twice and it is kept, while instcombine changes both functions and the tree is kept both times. So invalidation follows neither the identity of the pass nor the fact that the IR changed. | observed | cell same_pass_two_answers<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, the same four rows on both |
| What survives a pass is decided by the PreservedAnalyses the pass returns. The pass manager hands that set to the analysis manager, which drops everything the set does not cover. Nothing compares the IR before and after. | cited | cells same_pass_two_answers and the_flag_that_proves_it<br>llvm/docs/NewPassManager.md:288-306@llvmorg-23.1.0, "The pass manager will call the analysis manager's invalidate() method with the pass's returned PreservedAnalyses" |
| SimplifyCFGPass::run returns PreservedAnalyses::all() when simplifyFunctionCFG reports no change, and otherwise returns a PreservedAnalyses that contains the dominator tree only if RequireAndPreserveDomTree is set. | cited | cell same_pass_two_answers<br>llvm/lib/Transforms/Scalar/SimplifyCFGPass.cpp:374-391@llvmorg-23.1.0 |
| InstCombinePass::run also returns PreservedAnalyses::all() when it changed nothing, and when it did change something it preserves the CFGAnalyses set, which covers every analysis that depends on nothing but control flow and so keeps the dominator tree alive. EarlyCSE ends the same way. | cited | cell same_pass_two_answers<br>llvm/lib/Transforms/InstCombine/InstructionCombining.cpp:6301-6313@llvmorg-23.1.0 and llvm/lib/Transforms/Scalar/EarlyCSE.cpp:1820-1826@llvmorg-23.1.0, both ending in PA.preserveSet<CFGAnalyses>() |
| Running simplifycfg on sum_to with -simplifycfg-require-and-preserve-domtree produces the same IR as running it without, and the dominator tree is no longer thrown away. | observed | cell the_flag_that_proves_it<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, identical function bodies and no invalidation line on both |
| RequireAndPreserveDomTree is a hidden command line flag that is off by default, described in its own declaration as a temporary development switch for gradually uplifting SimplifyCFG into preserving the dominator tree. | cited | cell the_flag_that_proves_it<br>llvm/lib/Transforms/Utils/SimplifyCFG.cpp:101-107@llvmorg-23.1.0, "Temporary development switch used to gradually uplift SimplifyCFG into preserving DomTree," |

## t07_which_one_did_it

| Claim | Confidence | Evidence |
|---|---|---|
| A million deep tail recursion compiled at -O0 dies on a signal under lli, and the same source through default<O2> returns the right answer. The signal number is not the same on the two platforms, which is why the lesson prints it. | observed | cell two_answers<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, signal 4 on macOS and signal 11 on Linux, and 1 returned from the optimised build on both |
| -opt-bisect-limit numbers pass runs rather than pipeline entries, so the count is larger than the number of entries in the printed default<O2> string. For this two function module it is 192 runs against 120 entries on macOS and 191 against 119 on Linux. | observed | cell the_numbering<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0 |
| -opt-bisect-limit=N is a cap, not only a numbering. It is turned into the interval 1 to N, a limit of 0 means run no passes, and the default of -1 means run all of them. | cited | cells the_numbering and over_the_limit<br>llvm/lib/IR/OptBisect.cpp:31-49@llvmorg-23.1.0, "Convert limit to interval 1-Limit" with the 0 and -1 cases above it |
| Every pass above the limit prints a line saying it was not run, so the log distinguishes a pass that ran from one that was skipped rather than leaving it out. | cited | cell over_the_limit<br>llvm/lib/IR/OptBisect.cpp:89-94@llvmorg-23.1.0, printPassMessage writes "BISECT: " then "NOT " when the pass is not running, then the number, the name and the thing it was handed |
| Because the limit admits a prefix of the runs, any yes or no question about the output is monotone in the limit, so a binary search over it finds the exact run that changes the answer. Here eight probes are enough. | observed | cell the_search<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, eight probes on both, converging on run 37 |
| The run that turns the crash into an answer is number 37, tailcallelim on down, on both platforms, even though the two platforms disagree about the total number of runs. | observed | cell the_search<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, "BISECT: running pass (37) tailcallelim on down" on both |
| -opt-disable takes pass names and switches them off wherever they appear in the pipeline. Running default<O2> with tailcallelim disabled brings the crash back, and disabling simplifycfg, which appears eight times in the same pipeline, changes nothing. | cited | cell take_it_away<br>llvm/lib/IR/OptBisect.cpp:82-87@llvmorg-23.1.0 for the flag, and measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0 for the three outcomes |
| tailcallelim run on its own over the -O0 IR, with nothing else from -O2, is enough to make the program return. So the pass is both necessary within -O2 and sufficient without it. | observed | cells take_it_away and the_suspect_alone<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, the same exit code and the same transformed IR on both |
| The transformation replaces a self recursive call followed by a return with a branch to the entry of the function, which makes a loop. In the output the call is gone, there is a block named tailrecurse, and there is one phi at the top of it per parameter. | cited | cell the_suspect_alone<br>llvm/lib/Transforms/Scalar/TailRecursionElimination.cpp:9-11@llvmorg-23.1.0, "transforms calls of the current function (self recursion) followed by a return instruction with a branch to the entry of the function, creating a loop" |
| The transformed function still has its allocas and its loads and stores, because mem2reg is not in this pipeline. Running one pass runs one pass. | observed | cell the_suspect_alone<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, two allocas still present in down on both |

## t08_o2_twice

| Claim | Confidence | Evidence |
|---|---|---|
| Running default<O2> twice over sum_to gives a function one instruction shorter than running it once, 16 then 15, and a third run changes nothing. So the fixed point exists here and is two applications away rather than one. | observed | cell run_it_twice<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, the same counts and the same bodies on both |
| The unoptimised function, the one run version and the twice run version all return 181 for sum_to(100) modulo 251, which is the sum of nought to ninety nine reduced. | observed | cell all_three_agree<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, exit code 181 from lli in all three cases on both |
| The T07 procedure works with any yes or no property, and searching for the smallest prefix of the second -O2 whose output already equals the final body lands on gvn. | observed | cell where_did_it_change<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, gvn on sum_to on both |
| The four adds outside the loop appear at the run of indvars, which is where the loop is replaced by a closed form computed before it. | cited | cell the_two_adds<br>llvm/lib/Transforms/Scalar/IndVarSimplify.cpp:9-13@llvmorg-23.1.0, the pass "analyzes and transforms the induction variables (and computations derived from them) into simpler forms suitable for subsequent analysis and transformation" |
| gvn eliminates fully redundant instructions by numbering them, so it removes a second computation of an expression that is already there and does not rearrange an expression to create one. | cited | cell the_two_adds<br>llvm/lib/Transforms/Scalar/GVN.cpp:9-10@llvmorg-23.1.0, "performs global value numbering to eliminate fully redundant instructions" |
| Rearranging (n + x) - 1 into (n - 1) + x is reassociate's job, and it exists to put expressions into an order that lets constant propagation and common subexpression elimination fire. | cited | cell the_two_adds<br>llvm/lib/Transforms/Scalar/Reassociate.cpp:9-12@llvmorg-23.1.0, "reassociates commutative expressions in an order that is designed to promote better constant propagation, GCSE, LICM, PRE, etc." with the example "4 + (x + 5) -> x + (4 + 5)" |
| In the first -O2, reassociate is run 32 and indvars is run 47, so the expression reassociate would have rearranged does not exist until fifteen runs after reassociate has finished. Both numbers are the same on both platforms even though the totals are not. | observed | cell the_two_adds<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, run 32 and run 47 on both, against 111 and 110 total runs |
| gvn runs after indvars in the first -O2 and sees the closed form, and does not fold it, because at that point the two expressions are not written the same way. Nothing was skipped and no analysis was stale. | observed | cell the_two_adds<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, the same four adds before and after the run of gvn on both |
| Appending reassociate, instcombine and gvn to one default<O2> reproduces the body two default<O2>s produce, and no smaller subset of the three does. reassociate alone leaves the count unchanged, gvn alone finds nothing, and reassociate with gvn gets the count right and the flags wrong. | observed | cell the_repair<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, the same four outcomes on both |
| When gvn replaces one instruction with another it patches the survivor so it is not more restrictive than the value it replaced, by intersecting the two sets of flags. That is why merging an add nsw with an add that has no nsw leaves an add with no nsw, and why instcombine has to run in between to put nsw back on the new one. | cited | cell the_repair<br>llvm/lib/Transforms/Utils/Local.cpp:3190-3208@llvmorg-23.1.0, "Patch the replacement so that it is not more restrictive than the value being replaced" followed by ReplInst->andIRFlags(I), reached from llvm/lib/Transforms/Scalar/GVN.cpp:2234-2235@llvmorg-23.1.0 |

## t09_the_machine_in_the_ir

| Claim | Confidence | Evidence |
|---|---|---|
| One line of C compiled for x86_64-unknown-linux-gnu, aarch64-unknown-linux-gnu, thumbv7m-none-eabi and wasm32-unknown-unknown produces four different target datalayout strings, differing in pointer width, integer alignments, stack alignment and the list of native integer widths. | observed | cell two_headers<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, the same four strings on both |
| The data layout is a property of the module, written into the file as a string, rather than something recomputed by each consumer of the IR. | cited | cell two_headers<br>llvm/docs/LangRef.md:3447-3453@llvmorg-23.1.0, "A module may specify a target-specific data layout string that specifies how data is to be laid out in memory", with the syntax target datalayout = "layout specification" |
| The target triple is likewise a string in the module, of the form architecture-vendor-operating system with an optional environment. | cited | cells two_headers and the_triple_alone<br>llvm/docs/LangRef.md:3674-3690@llvmorg-23.1.0, "A module may specify a target triple string that describes the target host" and the canonical forms ARCHITECTURE-VENDOR-OPERATING_SYSTEM and ARCHITECTURE-VENDOR-OPERATING_SYSTEM-ENVIRONMENT |
| sizeof(long) is 8 on the two 64 bit targets and 4 on thumb and wasm, sizeof(void *) follows, and struct { char; long; } is 16 bytes aligned to 8 on the first two and 8 aligned to 4 on the other two. All four are constants in the IR before any pass runs, because the front end folded them. | observed | cell what_the_layout_says<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, the same four packed constants on both |
| A function declared to take and return long compiles to one taking and returning i64 on x86_64 and aarch64 and i32 on thumbv7m and wasm32. The C type does not survive into the IR; only a width does. | observed | cell what_the_layout_says<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, i64 twice then i32 twice on both |
| A struct of two ints passed by value becomes a single i64 parameter on x86_64 and aarch64, a [2 x i32] on thumbv7m, and a ptr noundef byval(%struct.Pair) on wasm32. A struct of four doubles becomes ptr byval on x86_64 and wasm32 and [4 x double] alignstack(8) on aarch64. The named struct type survives only where the argument is passed in memory. | observed | cell the_abi_is_in_the_signature<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, the same eight signatures on both |
| default<O2> as opt builds it has 120 entries and 4791 characters for a module whose triple is aarch64-unknown-linux-gnu, and 119 entries and 4770 characters for x86_64-unknown-linux-gnu, thumbv7m-none-eabi and wasm32-unknown-unknown. All four numbers come out of one binary on one host. | observed | cell the_pipeline_is_not_portable<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, all four counts reproduced on each host |
| Taking the multiset of pass names in each string and subtracting gives loop-idiom-vectorize once on the aarch64 side and nothing on the x86_64 side. It is not a reordering and not a repetition count, and 21 characters is the length of the name plus the separating comma. | observed | cell the_pipeline_is_not_portable<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, the same one element difference on both |
| The pair of numbers recorded in every claims file from T05 onward, 120 against 119 entries and 4791 against 4770 characters, is a difference between the aarch64 and x86_64 targets and not between macOS and Linux or between two LLVM builds. | inferred | cell the_pipeline_is_not_portable<br>the four target measurements above reproduce both members of the pair on each host, and the earlier lessons compiled for the host in each case, macOS arm64 giving the aarch64 numbers and Linux x86_64 the x86_64 ones |
| opt can only apply a target's callbacks if that target was built into it. A build without the ARM backend prints "failed to create target machine for 'thumbv7m-unknown-none-eabi'" on stderr for the thumb module and builds the generic 119 entry pipeline instead. The Homebrew build has every target and prints nothing. | observed | cell the_pipeline_is_not_portable<br>measured on Linux x86_64 with the min bundle's own opt from llvmorg-23.1.0, which warns for thumbv7m, and on macOS arm64 Homebrew LLVM 23.1.0, which does not. Both build 119 entries for that module, for different reasons. |
| Replacing only the target triple line of an x86_64 module with the aarch64 one, leaving the x86_64 data layout in place, produces the 120 entry pipeline with loop-idiom-vectorize in it. Deleting both target lines produces the 119 entry pipeline. | observed | cell the_triple_alone<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, the same three outcomes on both |
| AArch64TargetMachine::registerPassBuilderCallbacks registers a late loop optimisations callback that adds LoopIdiomVectorizePass at any optimisation level other than O0. The pipeline is built with target supplied callbacks rather than being a fixed list. | cited | cell the_triple_alone<br>llvm/lib/Target/AArch64/AArch64TargetMachine.cpp:620-628@llvmorg-23.1.0, registerPassBuilderCallbacks calling PB.registerLateLoopOptimizationsEPCallback with a lambda that does LPM.addPass(LoopIdiomVectorizePass()) when Level is not OptimizationLevel::O0 |
| The late loop optimisations extension point is documented as the last point in the loop optimisation pipeline before loop deletion, and as the place to add passes that can remove loops, such as target-specific loop idiom recognition. | cited | cell the_triple_alone<br>llvm/include/llvm/Passes/PassBuilder.h:429-441@llvmorg-23.1.0, "This extension point allows adding late loop canonicalization and simplification passes. This is the last point in the loop optimization pipeline before loop deletion" and "This is the place to add passes that can remove loops, such as target-specific loop idiom recognition" |
| All four targets optimise the counted loop to the same sixteen instructions, so the extra pass on aarch64 ran and changed nothing. The pipelines differ without the output differing for this function. The only remaining difference between the four definitions is the linkage word, dso_local on three targets and hidden on wasm32, which the front end chose. | observed | cell and_yet_the_body_is_the_same<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, four identical instruction lists on both |

## t10_the_names

| Claim | Confidence | Evidence |
|---|---|---|
| Pass names are matched as exact strings. instcomb, InstCombine and inst-combine are all rejected with unknown pass name, while instcombine is accepted. There is no case folding, no abbreviation and no suggestion of a near miss. | observed | cell four_ways_to_be_wrong<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, the same four verdicts on both |
| instcombine<foo> is rejected with invalid InstCombine pass parameter 'foo' rather than with unknown pass name, so the message distinguishes a name that was never recognised from one that was recognised and then given a parameter it does not take. instcombine<max-iterations=3> is accepted. | observed | cell four_ways_to_be_wrong<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, the same two verdicts on both |
| The unknown pass name message comes from the module level parser, after it has tried the CGSCC, function, loop nest, loop and machine function levels in that order and none of them recognised the name. The same function prints unknown pipeline name instead when the failing entry had a nested pipeline. | cited | cells four_ways_to_be_wrong and downhill_only<br>llvm/lib/Passes/PassBuilder.cpp:2707-2749@llvmorg-23.1.0, PassBuilder::parsePassPipeline for a ModulePassManager, ending in formatv("unknown {} name '{}'", (InnerPipeline.empty() ? "pass" : "pipeline"), FirstName) |
| opt --print-passes prints the vocabulary in sections by level, module, CGSCC, function, loop nest and loop, each with its passes, its passes that take parameters and its analyses, plus machine module and machine function sections marked (WIP). Function passes are the largest group by a wide margin. | observed | cell the_printed_vocabulary<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, the same section headings on both and function passes the largest group on both |
| A backend contributes pass names through its own registry file. AArch64PassRegistry.def lists aarch64-sve-shuffle-opts as its one LOOP_PASS, and X86PassRegistry.def lists x86-partial-reduction and x86-lower-amx-type among its FUNCTION_PASS lines. | cited | cell the_target_brings_names<br>llvm/lib/Target/AArch64/AArch64PassRegistry.def:27-31@llvmorg-23.1.0 for the one LOOP_PASS line, and llvm/lib/Target/X86/X86PassRegistry.def:22-27@llvmorg-23.1.0 for the three FUNCTION_PASS lines including x86-lower-amx-type and x86-partial-reduction |
| The backend pulls its registry in by defining GET_PASS_REGISTRY to the file name and including TargetPassRegistry.inc, which includes that file repeatedly with the macros defined differently each time, once to build a name matcher and once to construct the pass. This is the same function T09 quoted for the extension point. | cited | cell the_target_brings_names<br>llvm/lib/Target/AArch64/AArch64TargetMachine.cpp:620-622@llvmorg-23.1.0 for the define and include, and llvm/include/llvm/Passes/TargetPassRegistry.inc:37-137@llvmorg-23.1.0 for the repeated includes of GET_PASS_REGISTRY under different macro definitions |
| x86-partial-reduction and x86-lower-amx-type are accepted for a module whose triple is x86_64-unknown-linux-gnu and rejected as unknown pass name for one whose triple is aarch64-unknown-linux-gnu. aarch64-sve-shuffle-opts is accepted and rejected the other way round. All six results come from one opt binary. | observed | cell the_target_brings_names<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, the same six verdicts on both |
| None of those three target pass names appears in opt --print-passes, because that listing is printed without an input file and therefore without a triple to select a backend. | observed | cells the_printed_vocabulary and the_target_brings_names<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, an empty list on both |
| A narrower pass named in a wider place is wrapped for you, so instcombine and cgscc(instcombine) are both accepted. A wider pass named in a narrower place is an error, so function(globalopt) fails with unknown function pass and loop(instcombine) with unknown loop pass. The error names the level whose parser gave up. | cited | cell downhill_only<br>llvm/lib/Passes/PassBuilder.cpp:2719-2735@llvmorg-23.1.0 for the wrapping, trying module, CGSCC, function, loop nest, loop and machine function in that order, and llvm/lib/Passes/PassBuilder.cpp:2485-2487@llvmorg-23.1.0 for the unknown function pass message |
| Printing the pipeline built from a single name shows the adaptors that were inserted. globalopt stays as globalopt, instcombine becomes function(instcombine<max-iterations=1;verify-fixpoint>) and licm becomes function(loop-mssa(licm<allowspeculation>)), which is two levels of adaptor and the memory analysis variant. | observed | cell downhill_only<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, the same three strings on both |
| The parser hardcodes two names, licm and lnicm, as requiring the loop-mssa adaptor rather than loop. The check happens before any registry is consulted, which is why a bare licm comes out wrapped in loop-mssa. | cited | cell licm_is_special<br>llvm/lib/Passes/PassBuilder.cpp:1984-1993@llvmorg-23.1.0, isLoopPassName setting UseMemorySSA to true when checkParametrizedPassName matches "licm", with the same for "lnicm" in isLoopNestPassName above it |
| Writing loop(licm) explicitly overrides that choice. It parses, and then LICMPass::run finds no MemorySSA and reports the fatal usage error LICM requires MemorySSA (loop-mssa). loop(loop-rotate) is accepted and runs, so the loop adaptor itself is fine. | cited | cell licm_is_special<br>llvm/lib/Transforms/Scalar/LICM.cpp:303-307@llvmorg-23.1.0, LICMPass::run beginning with if (!AR.MSSA) reportFatalUsageError("LICM requires MemorySSA (loop-mssa)") |

## t11_not_what_clang_optimised

| Claim | Confidence | Evidence |
|---|---|---|
| For a loop, a conditional, a floating point expression, a self recursive function and a switch, clang -O2 and clang -O0 followed by opt -passes=default<O2> produce the same instructions in the same order, once value names are normalised away. | observed | cell five_that_agree<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, five agreements on both |
| For a module with a static helper called twice, clang -O2 inlines the helper and folds the result to 6n + 3, while the -O0 then opt route leaves both calls in place. The pipeline contains the inliner in both cases. | observed | cell the_call_that_did_not_inline<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, the same two bodies on both |
| clang at -O0 gives every function the attributes noinline, nounwind and optnone, among others that depend on the platform. Passing -Xclang -disable-O0-optnone removes optnone and leaves noinline untouched. | observed | cell what_the_o0_file_says<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, the same three attributes present and the same one removed on both |
| irx.compile_c invokes clang with -Xclang -disable-O0-optnone, which is why every lesson in this book has been able to run passes over -O0 output at all. | cited | cell what_the_o0_file_says<br>toolkit/irx/ir.py:148-156, Module.from_c passing "-Xclang", "-disable-O0-optnone" with a comment saying that without it clang marks the functions |
| Running default<O2> over IR that still carries optnone leaves the function with the instruction count it started with. Around ninety passes are skipped with a message naming the attribute, and the forty that run are module level passes, which are handed the module rather than a function. | observed | cell optnone_is_a_wall<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, 26 instructions before and after on both, and the same first two skip lines |
| The skipping is not implemented inside each pass. OptNoneInstrumentation registers a single should run callback with the pass instrumentation machinery, which answers false for any function, loop or machine function whose function has optnone, and prints the message the lesson shows when debug logging is on. | cited | cell optnone_is_a_wall<br>llvm/lib/Passes/StandardInstrumentations.cpp:1032-1053@llvmorg-23.1.0, registerCallbacks calling registerShouldRunOptionalPassCallback and shouldRun testing F->hasOptNone() before printing "Skipping pass ... due to optnone attribute" |
| optnone means most optimisation passes skip the function, with interprocedural passes excepted, and it requires noinline to be present as well so that the function is never inlined into any caller. Removing optnone therefore leaves a function that is still closed to the inliner. | cited | cells what_the_o0_file_says and putting_the_inlining_back<br>llvm/docs/LangRef.md:2545-2556@llvmorg-23.1.0, "This function attribute indicates that most optimization passes will skip this function" and "This attribute requires the noinline attribute to be specified on the function as well, so the function is never inlined into any caller" |
| Running forceattrs with -force-remove-attribute=g:noinline before default<O2> makes the -O0 route produce the same instructions as clang -O2, which confirms that the missing attribute removal, and nothing else, was the cause. | cited | cell putting_the_inlining_back<br>llvm/lib/Transforms/IPO/ForceFunctionAttrs.cpp:20-38@llvmorg-23.1.0 for the force-attribute and force-remove-attribute options, documented with the example -force-attribute=foo:noinline, and measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0 |
| clang emits no !tbaa metadata at -O0 and emits it at -O2. For a function that stores through an int pointer, stores through a float pointer and then loads the first again, clang -O2 folds the load to the constant and the -O0 then opt route keeps the load. | observed | cell the_metadata_that_is_not_there<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, two tbaa nodes and ret i32 1 from clang -O2, none and a surviving load from the other route |
| clang -O2 -fno-strict-aliasing emits no !tbaa and keeps the load, landing on the same body as the -O0 then opt route. So the difference is the metadata in the input and not the passes that ran. | observed | cell the_metadata_that_is_not_there<br>measured on macOS arm64 LLVM 23.1.0 and Linux x86_64 LLVM 23.1.0, the same three rows on both |
| LLVM IR does not put types on memory, so type based alias analysis is driven by metadata the front end attaches, describing the type system of the source language, which TypeBasedAliasAnalysis reads back. | cited | cell the_metadata_that_is_not_there<br>llvm/lib/Analysis/TypeBasedAliasAnalysis.cpp:9-20@llvmorg-23.1.0, "In LLVM IR, memory does not have types, so LLVM's own type system is not suitable for doing TBAA. Instead, metadata is added to the IR to describe a type system of a higher level language." |

## x03_your_first_fold

| Claim | Confidence | Evidence |
|---|---|---|
| At -O1 clang turns `a > 0 && b > 0` into a single `and i1` and leaves `a > 0 && (a << b) > 5` as a `select i1`. | observed | cell two_shapes<br>llvm/lib/Transforms/InstCombine/InstCombineSelect.cpp:3754-3806@llvmorg-23.1.0 |
| A shift whose count is equal to or larger than the width of the value being shifted produces poison, which is why %sh in this lesson can be poison. | cited | cell check<br>llvm/docs/LangRef.md:11170-11173@llvmorg-23.1.0 |
| Replacing `select i1 %ok, i1 %big, i1 false` with `and i1 %ok, %big` is not a valid refinement, and Alive2 gives a = 0, b = 32 as the input that breaks it. | observed | cell check<br> |
| The original and the naive fold print the same answer for every one of the six test inputs, including the counterexample a = 0, b = 32, because poison is not a value lli can produce or observe. | observed | cell tests<br> |
| The and version can be replaced by the select version and Alive2 verifies it, which is the same pair of files that failed in the other direction. | observed | cell backwards<br> |
| Freezing the true arm before the `and` makes the fold verify, in under a tenth of a second. | observed | cell check2<br>llvm/docs/LangRef.md:13436@llvmorg-23.1.0 |
| InstCombine performs both this fold and its `or` mirror only when impliesPoisonOrCond proves the arm cannot be poison, and the comment above the pair says so in as many words. | cited | cell instcombine<br>llvm/lib/Transforms/InstCombine/InstCombineSelect.cpp:3754-3806@llvmorg-23.1.0 |
| Swapping the `shl` for an `add` with no nsw makes real instcombine perform the fold, and putting the `shl` back makes it decline again. | observed | cell instcombine<br> |
| Alive2 finds the counterexample for the wrong sdiv fold at every width in hundredths of a second, and proving the correct version cliffs between i8, which it does in about 0.04s, and i32, where it exhausts a five second budget. | observed | cell bound, and docs/probes/alive.md for the wider sweep<br> |
| A timeout is a fourth answer alongside correct, wrong and error, and it means nothing was proved in either direction. | observed | cell bound<br>toolkit/irx/verify.py and toolkit/tests/test_verify.py |
| The pass plugin header is llvm/Plugins/PassPlugin.h in LLVM 23, not llvm/Passes/PassPlugin.h. | observed | cell plugin<br>llvm/include/llvm/Plugins/PassPlugin.h@llvmorg-23.1.0 |
| Both plugins in this lesson are loaded into one opt process, and without an anonymous namespace the second pass class resolves to the first one's symbols and silently runs the wrong code. | observed | measured on macOS arm64 while writing this lesson, which is how the bug was found<br> |
| Each %%irxplug cell compiles and loads in under two seconds, well inside the twenty second budget the lesson was designed against. | observed | cell plugin, cell plugin2<br> |

